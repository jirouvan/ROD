import mmdet
import torch
import numpy as np

import torch

# x = int(torch.tensor(0))
# print(x)
for a,b,c in zip([1,2,3,4,5],[4,5,6],[7,8,9]):
    print(a)
    print(b)
    print(c)


# device = torch.device("cuda" if torch.cuda.is_available() else "cpu")
# cls_assigned_scores = torch.zeros((16,8400,10), device=device)
# print(cls_assigned_scores.shape)
# x=torch.tensor([])
# y=torch.tensor([1])
# print(x)
# x=x.view(1, -1, 1)
# y=y.view(1, -1, 1)
# x = torch.cat((x, y), dim=1)
# print(x)


# a1 = torch.tensor([[[2],[3],[4]]])
# a = torch.tensor([[[5],[6],[7]]])
#
# b = []
# b.append(a1)
# b.append(a)
#
# c = torch.concat(b)
#
# print(c)
